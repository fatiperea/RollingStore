function login() {
    let user.getElementById("usuario").value;
    let pass.getElementById("pass").value;

    if (user=="admin" && pass=="123") {
        window.location="administracion.html"
    } else {
        alert("Usuario o contraseña incorrecta");
    }
}